import { AppLayout } from "../layout/AppLayout";

export function CartPage() {
    return (
        <AppLayout>
            <h1 className="text-2xl font-semibold">Carrito</h1>
            <p className="mt-2 text-slate-600">Siguiente: carrito funcional</p>
        </AppLayout>
    );
}