import { AppLayout } from "../layout/AppLayout";

export function CatalogPage() {
    return (
        <AppLayout>
            <h1 className="text-2xl font-semibold">Catálogo</h1>"
            <p className="mt-2 text-slate-600">Siguiente: productos desde JSON.</p>
        </AppLayout>
    );
}